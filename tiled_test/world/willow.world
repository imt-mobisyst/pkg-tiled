# cave.world
# simple cave environment with a pioneer robot based on the basic world file examples of 
# Richard Vaughan Richard Vaughan, Andrew Howard, Luis Riazuelo
# Authors: Markus Bader

include "include/robots.inc"
include "include/maps.inc"

# set the resolution of the underlying raytrace model in meters
resolution 0.02

# simulation timestep in milliseconds
interval_sim 100  

# configure the GUI window
window
(
  size [ 635.000 666.000 ] # in pixels
  scale 28   # pixels per meter
  center [ 8  10 ]
  rotate [ 0  0 ]

  show_data 1              # 1=on 0=off
)

# load an environment bitmap
willow_map( name "office" )

# Throw blocks
small_block( pose [ 5 2.5 0 180.000 ] color "green")

# throw in a robot
pioneer2dx_with_laser 
(
  name "robot_0"
  color "red" 
  pose [ 50 30 0 24 ] 
)
